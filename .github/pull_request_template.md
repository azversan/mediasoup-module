## Summary

<!--
  A concise description of what this PR does and why.
  If this fixes a bug, link the issue: "Fixes #123"
  If this implements a feature request, link it: "Closes #456"
-->

Fixes # (issue)

---

## Type of change

<!--
  Check all that apply. Delete options that are not relevant.
-->

- [ ] 🐛 Bug fix (non-breaking change that fixes an issue)
- [ ] ✨ New feature (non-breaking change that adds functionality)
- [ ] 💥 Breaking change (fix or feature that would cause existing functionality to change)
- [ ] 🔧 Refactor (no functional change, code quality improvement)
- [ ] 📖 Documentation update
- [ ] 🧪 Tests only (no production code changed)
- [ ] 🔨 Build / CI / tooling

---

## Changes

<!--
  List the specific files and changes made. Be explicit about what changed and why.
  Example:
  - `MediasoupService`: added `pipeToRouter()` helper
  - `mediasoup.interface.ts`: extended `RouterOptions` with `pipeOptions` field
  - `observer.service.ts`: handle `pipetransport` observer events
-->

-
-
-

## How has this been tested?

<!--
  Describe the tests that cover your changes.
  If you added new unit tests, mention which spec file(s).
  If manual testing was done, describe the steps.
-->

- [ ] Unit tests added / updated (`src/**/__tests__/**`)
- [ ] All existing tests pass (`npm test`)
- [ ] Manual testing performed

**Test details:**

```
# Paste relevant test output or describe manual steps here
```

---

## Checklist

<!-- Go through each item and check the ones that apply. -->

### Code quality

- [ ] My code follows the existing style (ESLint + Prettier pass: `npm run lint`)
- [ ] TypeScript compiles without errors (`npx tsc --project tsconfig.build.json --noEmit`)
- [ ] I have added JSDoc comments to all new public methods and interfaces
- [ ] No console.log or debug statements left in production code

### Testing

- [ ] New and existing unit tests pass (`npm test`)
- [ ] Test coverage has not decreased
- [ ] Edge cases and error paths are covered

### Documentation

- [ ] README is updated (if the public API changed)
- [ ] CHANGELOG is updated (if applicable)
- [ ] Breaking changes are clearly documented

### Compatibility

- [ ] This change is backward-compatible **OR** I have labelled this PR as a breaking change
- [ ] Peer dependency constraints are still satisfied

---

## Screenshots / logs (if applicable)

<!--
  For UI-related changes or when the output is visual, add screenshots here.
  For runtime behavior changes, paste relevant log output.
-->

---

## Additional notes

<!--
  Anything else the reviewer should know: context, trade-offs, follow-up tasks, etc.
-->
